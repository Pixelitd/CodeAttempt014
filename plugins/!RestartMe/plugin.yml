name: RestartMe
main: restartme\RestartMe
version: 2.0.5
api: [1.10.0, 2.0.0]
load: STARTUP
author: GamerCrafter, AnyaTatanova
description: "Make sure your server stays up all the time!"
website: https://github.com/Viperize/RestartMe-PocketMine
permissions:
 restartme:
  default: op
  children:
   restartme.command:
    default: op
    children:
     restartme.command.restartme:
      default: op
